# E2E Test Cases — Aomaoi (Sugarcane Farm Accounting)

Lab Week 7 (ITDS362) — Cypress E2E

| Part | Role under test | ID prefix | Spec folder |
|---|---|---|---|
| A | Superadmin | `SA-` | `cypress/e2e/superadmin/` |
| B | Admin | `AD-` | `cypress/e2e/admin/` |

Worker/auth E2E is owned by a teammate — out of scope here. Only Part A + Part B are implemented in this repo.

## Test data strategy

- DB starts with only `superadmin / 1234`. Every spec builds its own data through the API (`cy.request` to `http://localhost:8081/api`) in the order **farm → admin → worker → work log**.
- Every run uses a unique `runId` (timestamp in base36), used only in usernames and farm names so the suite can be re-run without cleanup. Full names / nicknames contain **no digits** (zod rule `^[^0-9]*$`).
- Seeded admins/workers get their temp password changed via `POST /auth/change-password` so `isFirstLogin = false` (the Set New Password modal would otherwise block the page). Only first-login tests keep a fresh account.
- Tests that are not about the login form log in with `cy.session` + writing `localStorage['auth-storage']` in zustand-persist shape: `{"state":{"currentUser":{...},"token":"...","isAuthenticated":true},"version":0}`.
- Selectors: `data-test` attributes via `cy.getByTestID(id)`. Elements generated by libraries (react-datepicker calendar days) use the library class names.

Expected results describe the **correct** behaviour. A test that fails because the app does not meet it is reported as an app bug, not changed to pass.

---

# Part A — Superadmin

## A1. Login / logout / route guard

| ID | Title | Preconditions | Steps | Input Data | Expected Result |
|---|---|---|---|---|---|
| SA-AUTH-001 | Login succeeds as superadmin | Backend running; seeded superadmin | 1) Visit `/login`<br>2) Type username/password<br>3) Click LOGIN | `superadmin` / `1234` | Redirect to `/dashboard`; superadmin sidebar shows All Farms / All Admins / Audit Logs; `auth-storage` in localStorage has `isAuthenticated: true`, role `superadmin` |
| SA-AUTH-002 | Login fails with wrong password | Seeded superadmin | 1) Visit `/login`<br>2) Type correct username, wrong password<br>3) Click LOGIN | `superadmin` / `wrong-pass` | Error banner "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง"; stays on `/login`; not authenticated |
| SA-AUTH-003 | Login fails for unknown user | — | 1) Visit `/login`<br>2) Type unknown username<br>3) Click LOGIN | `nouser_<runId>` / `123456` | Same error banner; stays on `/login` |
| SA-AUTH-004 | Empty fields are blocked | — | 1) Visit `/login`<br>2) Click LOGIN with empty fields | (empty) | Form is invalid (HTML `required`); no login request is sent; stays on `/login` |
| SA-AUTH-005 | Logout clears the session | Logged in as superadmin | 1) Click "Log out" in sidebar<br>2) Visit `/dashboard` | — | Redirect to `/login`; `auth-storage` has `isAuthenticated: false`; visiting `/dashboard` redirects back to `/login` |
| SA-GUARD-001 | Protected routes need login | Not logged in | Visit each: `/dashboard`, `/farms`, `/admins`, `/audit-logs`, `/workers`, `/work`, `/history` | — | Every route redirects to `/login` |
| SA-GUARD-002 | Unknown route falls back | Logged in as superadmin | Visit `/some-unknown-page` | — | Redirect to `/dashboard` |
| SA-GUARD-003 | Superadmin cannot open admin/worker pages | Logged in as superadmin | Visit `/workers`, `/work`, `/history` | — | Each redirects to `/dashboard` showing the superadmin dashboard |
| SA-GUARD-004 | Sidebar shows only superadmin menus | Logged in as superadmin | Open `/dashboard` | — | Menus: Dashboard / All Farms / All Admins / Audit Logs; no Workers / Work Log / History |

## A2. Farm management

| ID | Title | Preconditions | Steps | Input Data | Expected Result |
|---|---|---|---|---|---|
| SA-FARM-001 | Add a farm | Logged in as superadmin; on `/farms` | 1) Click "เพิ่มฟาร์มใหม่"<br>2) Fill name and location<br>3) Click confirm | name `E2E Farm <runId>`, location `นครปฐม` | Success modal "เพิ่มฟาร์มสำเร็จ"; new farm card shows the name and location; "All Farms" count +1 |
| SA-FARM-002 | Farm form validation | On add farm modal | 1) Leave name and location empty<br>2) Click confirm | (empty) | Errors "กรุณากรอกชื่อฟาร์ม" and "กรุณากรอกที่ตั้ง"; modal stays open; no farm created |
| SA-FARM-003 | Edit a farm | Farm exists (API seeded) | 1) Click edit on the farm card<br>2) Change name and location<br>3) Click confirm | name `E2E Farm Edited <runId>`, location `ราชบุรี` | "แก้ไขข้อมูลสำเร็จ"; card shows the new name and location |
| SA-FARM-004 | Delete a farm | Farm exists (API seeded) | 1) Click delete on the farm card<br>2) Click confirm | — | "ลบฟาร์มสำเร็จ"; card no longer shown |
| SA-FARM-005 | Search farms | Two farms exist | Type part of one farm name in search | `<runId>-a` | Only the matching farm card is shown |

## A3. Admin management

| ID | Title | Preconditions | Steps | Input Data | Expected Result |
|---|---|---|---|---|---|
| SA-ADMIN-001 | Add an admin | Farm exists; on `/admins` | 1) Click "เพิ่ม Admin ใหม่"<br>2) Fill form, pick farm<br>3) Click confirm | fullName `สมหญิง ทดสอบ`, username `e2e_adm_<runId>`, phone `0812345678`, temp pw `Temp1234` | "เพิ่ม Admin สำเร็จ"; row shows name, username, phone, farm name badge, status Pending |
| SA-ADMIN-002 | Admin form validation | On add admin modal | Submit with: name containing digits, 9-digit phone, no farm, 5-char temp pw | `Admin1`, `081234567`, (no farm), `12345` | Errors "ชื่อ-นามสกุลต้องไม่มีตัวเลข", "เบอร์โทรต้องเป็นตัวเลข 10 หลัก", "กรุณาเลือกฟาร์ม", "ต้องมีอย่างน้อย 6 ตัวอักษร"; nothing created |
| SA-ADMIN-003 | Duplicate admin username rejected | Admin `e2e_adm_<runId>` exists | Add another admin with the same username | same username | Alert starting with "Failed to add admin"; only one row with that username |
| SA-ADMIN-004 | Edit an admin | Admin exists | 1) Click edit on row<br>2) Change phone<br>3) Confirm | phone `0898765432` | "แก้ไขข้อมูลสำเร็จ"; row shows new phone |
| SA-ADMIN-005 | Delete an admin | Admin exists | 1) Click delete on row<br>2) Confirm | — | "ลบ Admin สำเร็จ"; row status Inactive; manage column shows "-" |
| SA-ADMIN-006 | Filter admins by farm | Admins in farm A and farm B | Choose farm A in farm filter | farm A | Only admins of farm A listed |
| SA-ADMIN-007 | Deleted admin cannot log in | Admin deleted (status inactive) | 1) Visit `/login`<br>2) Log in as that admin | admin username / its password | Login rejected with error banner (delete dialog promises "จะไม่สามารถเข้าสู่ระบบได้อีก") |

## A4. Audit log

| ID | Title | Preconditions | Steps | Input Data | Expected Result |
|---|---|---|---|---|---|
| SA-AUDIT-001 | Admin actions are logged | During this run: admin created and deleted, worker created and password reset | Open `/audit-logs`, search `<runId>` | `<runId>` | Rows for CREATE_ADMIN, DELETE_ADMIN (by superadmin), CREATE_WORKER, RESET_PASSWORD (by the admin) with the target usernames |
| SA-AUDIT-002 | Refresh reloads logs | On `/audit-logs` | Create an admin via API, click "Refresh" | — | New CREATE_ADMIN row appears without page reload |

## A5. Superadmin-only API is protected from other roles

| ID | Title | Preconditions | Steps | Input Data | Expected Result |
|---|---|---|---|---|---|
| SA-API-001 | No token is rejected | — | `GET /api/farms` without `Authorization` | — | HTTP 401 |
| SA-API-002 | Admin cannot read audit logs | Admin token | `GET /api/superadmin/audit-logs` | admin JWT | HTTP 403 |
| SA-API-003 | Worker cannot read audit logs | Worker token | `GET /api/superadmin/audit-logs` | worker JWT | HTTP 403 |
| SA-API-004 | Admin cannot create farms | Admin token | `POST /api/farms` | `{name, location}` | HTTP 403; farm not created |
| SA-API-005 | Admin cannot create admins | Admin token | `POST /api/admins` | valid admin payload | HTTP 403; admin not created |

---

# Part B — Admin

## B1. Login / route guard

| ID | Title | Preconditions | Steps | Input Data | Expected Result |
|---|---|---|---|---|---|
| AD-AUTH-001 | First login must set a new password | Admin created by superadmin (API), never logged in | 1) Log in with temp password<br>2) Modal "Set New Password" appears<br>3) Submit wrong old password<br>4) Submit mismatched new passwords<br>5) Submit new password < 6 chars<br>6) Submit valid values<br>7) Close success modal, logout, log in with new password | temp `Temp1234`, new `NewPass123` | 3) alert "Failed to change password"; 4) alert "Passwords do not match"; 5) alert "Password must be at least 6 characters"; 6) "Add New Password Successfully!"; 7) login works with new password and the modal does **not** appear again |
| AD-AUTH-002 | Logout clears the session | Logged in as admin | Click "Log out" | — | Redirect to `/login`; visiting `/workers` redirects to `/login` |
| AD-GUARD-001 | Admin cannot open superadmin pages | Logged in as admin | Visit `/farms`, `/admins`, `/audit-logs` | — | Each redirects to `/dashboard` showing the admin dashboard |
| AD-GUARD-002 | Admin cannot open worker pages | Logged in as admin | Visit `/history`, `/balance` | — | Each redirects to `/dashboard` |
| AD-GUARD-003 | Sidebar shows only admin menus | Logged in as admin | Open `/dashboard` | — | Menus: Dashboard / Workers / Work Log; no All Farms / All Admins / Audit Logs |
| AD-DASH-001 | Dashboard "More" opens work log | Logged in as admin | Click "More" in Work Activity Log card | — | URL is `/work`; page title "Work Activity Log" |

## B2. Worker management

| ID | Title | Preconditions | Steps | Input Data | Expected Result |
|---|---|---|---|---|---|
| AD-WORKER-001 | Add a worker | Logged in as admin of farm A; on `/workers` | 1) Click "เพิ่มคนงานใหม่"<br>2) Fill form<br>3) Confirm | fullName `สมชาย ใจดี`, nickname `ชาย`, username `e2e_wkr_<runId>`, phone `0811111111`, temp pw `Temp1234` | "เพิ่มคนงานสำเร็จ"; row shows name, nickname, `@username`, phone, status Pending |
| AD-WORKER-002 | Worker form validation | On add worker modal | Submit with: digits in name and nickname, 2-char username, 9-digit phone, 5-char temp pw | `Somchai1`, `Chai2`, `ab`, `081111111`, `12345` | Errors "ชื่อ-นามสกุลต้องไม่มีตัวเลข", "ชื่อเล่นต้องไม่มีตัวเลข", "ต้องมีอย่างน้อย 3 ตัวอักษร", "เบอร์โทรต้องเป็นตัวเลข 10 หลัก", "ต้องมีอย่างน้อย 6 ตัวอักษร"; nothing created |
| AD-WORKER-003 | Username with space rejected | On add worker modal | Fill valid form but username with a space | `e2e wkr <runId>` | Alert starting with "Failed to add worker"; no row created |
| AD-WORKER-004 | Edit a worker | Worker exists | 1) Click edit<br>2) Change nickname and phone<br>3) Confirm | nickname `ชายใหม่`, phone `0822222222` | "แก้ไขข้อมูลสำเร็จ"; row shows new nickname and phone |
| AD-WORKER-005 | Reset worker password | Worker exists and already set own password | 1) Edit worker<br>2) Click "+ เปลี่ยนรหัสผ่านคนงาน"<br>3) Type new password, click "บันทึกรหัสผ่าน"<br>4) Worker logs in with new password | `Reset1234` | Alert "เปลี่ยนรหัสผ่านสำเร็จ!"; worker can log in with `Reset1234` and must set a new password (modal shown); old password no longer works |
| AD-WORKER-006 | Reset password too short | Edit worker modal open | Type 5-char password, click "บันทึกรหัสผ่าน" | `12345` | Alert "รหัสผ่านต้องมีอย่างน้อย 6 ตัวอักษร"; password unchanged |
| AD-WORKER-007 | Delete a worker | Worker exists | 1) Click delete<br>2) Confirm | — | "ลบคนงานสำเร็จ"; status Inactive; manage column shows "-" |
| AD-WORKER-008 | Deleted worker cannot log in | Worker deleted in AD-WORKER-007 | Log in as that worker | worker username / its password | Login rejected with error banner; stays on `/login` |
| AD-WORKER-009 | Admin sees only own farm's workers | Worker A1 in farm A, worker B1 in farm B | Admin A opens `/workers` | — | Worker A1 listed; worker B1 not listed |
| AD-WORKER-010 | Search and status filter | Workers with status Pending and Inactive | 1) Search by nickname<br>2) Set status filter Inactive | nickname, `Inactive` | 1) Only matching worker; 2) only inactive workers |

## B3. Work log

Wage formulas: cutting = rows × wa/row × 2, planting = furrows × wa/furrow × 2.5, watering = days × daily rate (days = end − start + 1), spraying = tanks × 150.

| ID | Title | Preconditions | Steps | Input Data | Expected Result |
|---|---|---|---|---|---|
| AD-WORKLOG-001 | Record cutting work | Admin with an active worker; on `/work` | 1) Click "บันทึกงานใหม่"<br>2) Pick worker, type ตัดอ้อย<br>3) Fill rows / wa per row<br>4) Check preview, confirm | rows `3`, wa/row `100`, date today | Preview "ค่าแรงรวม 600 บาท"; "บันทึกงานสำเร็จ"; table row: worker name, ตัดอ้อย, Qty `300`, Unit `วา`, Wages `฿600` |
| AD-WORKLOG-002 | Record planting work | Same | Same with ปลูกอ้อย | furrows `4`, wa/furrow `20` | Preview 200 บาท; row ปลูกอ้อย, Qty `80`, `วา`, `฿200` |
| AD-WORKLOG-003 | Record watering work with date range | Same | 1) Choose รดน้ำ<br>2) Pick start and end date in calendar<br>3) Daily rate<br>4) Confirm | start day 10, end day 14 of current month, rate `350` | Form has no "วันที่ทำงาน" field; preview "5 วัน × 350 บาท/วัน" and 1,750 บาท; row รดน้ำ, Qty `5`, Unit `วัน`, `฿1,750` |
| AD-WORKLOG-004 | Record spraying work | Same | Same with พ่นยา | tanks `3` | Preview 450 บาท; row พ่นยา, Qty `3`, `ถัง`, `฿450` |
| AD-WORKLOG-005 | Worker is required | Entry modal open | Confirm without picking a worker | — | Error "กรุณาเลือกคนงาน"; modal stays open |
| AD-WORKLOG-006 | Watering end date before start date | Watering selected | Pick start day 14, end day 10 | — | Error "วันที่สิ้นสุดต้องไม่ก่อนวันที่เริ่ม"; nothing saved |
| AD-WORKLOG-007 | Zero quantity rejected | Cutting selected | Type rows `0`, confirm | rows `0` | Error "ต้องมากกว่า 0"; nothing saved |
| AD-WORKLOG-008 | Server calculates wages itself | Admin token, worker exists | `POST /api/work-logs` for each type | cutting 3×100, planting 4×20, watering 2026-09-01→05 × 350, spraying 3 | Response `total` = 600 / 200 / 1750 / 450; watering `days` = 5 |
| AD-WORKLOG-009 | Filter work log by type and worker | Logs of 2 types for 2 workers | 1) Filter type ตัดอ้อย<br>2) Filter worker | — | Only matching rows shown |

## B4. Admin-only API is protected from other roles

| ID | Title | Preconditions | Steps | Input Data | Expected Result |
|---|---|---|---|---|---|
| AD-API-001 | No token is rejected | — | `GET /api/workers` without `Authorization` | — | HTTP 401 |
| AD-API-002 | Worker cannot create workers | Worker token | `POST /api/workers` | valid worker payload | HTTP 403; worker not created |
| AD-API-003 | Worker cannot record work logs | Worker token | `POST /api/work-logs` for itself | spraying, tanks `99` | HTTP 403; no log created |
| AD-API-004 | Admin cannot reset a worker of another farm | Admin A (farm A), worker B (farm B) | `POST /api/workers/{workerB}/reset-password` with admin A token | `{newPassword:"Hacked123"}` | HTTP 403/404; worker B can still log in with old password |

